{
  "name": "powerbank-bridge",
  "version": "1.0.0",
  "description": "Power Bank Sharing System — Node.js bridge between mobile app and manufacturer IoT server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node -e \"console.log('powerbank-bridge: no compile step')\""
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.4",
    "amqplib": "^0.10.3",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
