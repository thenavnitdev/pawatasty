# Stripe Payment System - Visual Guide

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────┐   │
│  │ PaymentMethods│  │ AddCardModal │  │ StripeCardInput    │   │
│  │ Component    │  │              │  │ (Stripe Elements)  │   │
│  └──────┬───────┘  └──────┬───────┘  └─────────┬──────────┘   │
│         │                  │                     │              │
│         └──────────────────┴─────────────────────┘              │
│                             │                                   │
└─────────────────────────────┼───────────────────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │   Stripe.js SDK    │
                    └─────────┬──────────┘
                              │
┌─────────────────────────────┼───────────────────────────────────┐
│                      EDGE FUNCTIONS                              │
├─────────────────────────────┼───────────────────────────────────┤
│                             │                                    │
│  ┌──────────────────────────▼───────────────────────────────┐  │
│  │         /functions/v1/payment-methods                     │  │
│  │  • GET  - List payment methods                            │  │
│  │  • POST - Add payment method                              │  │
│  │  • DELETE - Remove payment method                         │  │
│  │  • PUT - Set default / Complete setup                     │  │
│  └──────────────────────────┬────────────────────────────────┘  │
│                             │                                    │
│  ┌──────────────────────────▼────────────────────────────────┐ │
│  │         /functions/v1/subscription-payment                 │ │
│  │  • POST /create-intent - Create subscription payment       │ │
│  │  • POST /confirm - Confirm and activate subscription       │ │
│  │  • GET /verify - Verify payment status                     │ │
│  └──────────────────────────┬─────────────────────────────────┘ │
│                             │                                    │
│  ┌──────────────────────────▼────────────────────────────────┐ │
│  │         /functions/v1/unified-payment                      │ │
│  │  • POST /create-intent - Any payment context               │ │
│  │  • POST /confirm - Finalize payment                        │ │
│  │  • POST /charge-saved-method - Off-session charge          │ │
│  └──────────────────────────┬─────────────────────────────────┘ │
│                             │                                    │
│  ┌──────────────────────────▼────────────────────────────────┐ │
│  │         /functions/v1/stripe-webhook                       │ │
│  │  • Verifies webhook signatures                             │ │
│  │  • Handles setup_intent.succeeded                          │ │
│  │  • Handles setup_intent.setup_failed                       │ │
│  └──────────────────────────┬─────────────────────────────────┘ │
│                             │                                    │
└─────────────────────────────┼────────────────────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │   Stripe API       │
                    │  stripe.com/v1     │
                    └─────────┬──────────┘
                              │
                    ┌─────────▼─────────┐
                    │  Stripe Dashboard  │
                    │   (webhooks)       │
                    └─────────┬──────────┘
                              │
┌─────────────────────────────┼────────────────────────────────────┐
│                        DATABASE (Supabase)                        │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────┐  ┌─────────────────────┐               │
│  │ payment_methods    │  │ users                │               │
│  ├────────────────────┤  ├─────────────────────┤               │
│  │ id                 │  │ user_id             │               │
│  │ user_id            │  │ stripe_customer_id  │               │
│  │ type               │  │ subscription        │               │
│  │ stripe_pm_id       │  │ validation_fee_paid │               │
│  │ stripe_setup_id    │  └─────────────────────┘               │
│  │ status             │                                          │
│  │ is_primary         │  ┌─────────────────────┐               │
│  │ supports_*         │  │ user_memberships    │               │
│  └────────────────────┘  ├─────────────────────┤               │
│                          │ user_id             │               │
│  🔒 Row Level Security   │ membership_tier     │               │
│     auth.uid() checks    │ subscription_status │               │
│                          │ stripe_sub_id       │               │
│                          └─────────────────────┘               │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## Payment Flow Diagrams

### Flow 1: Add Credit Card

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Opens "Add Card" modal
     ▼
┌─────────────────┐
│ AddCardModal    │
└────┬────────────┘
     │ 2. User enters card details
     ▼
┌─────────────────┐
│ Stripe Elements │ ◄─── Stripe.js validates in real-time
└────┬────────────┘
     │ 3. Creates Payment Method (pm_xxx)
     ▼
┌─────────────────────────┐
│ POST /payment-methods   │
└────┬────────────────────┘
     │ 4. Validates with Stripe API
     │ 5. Attaches to customer
     │ 6. Creates €0.50 validation charge
     ▼
┌─────────────────┐
│ Database        │ ◄─── Saves with status=active
└────┬────────────┘
     │ 7. Returns success
     ▼
┌─────────────────┐
│ UI Updates      │ ◄─── Card appears in list
└─────────────────┘
```

### Flow 2: Add iDEAL (Redirect Flow)

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Selects "iDEAL"
     ▼
┌──────────────────────┐
│ POST /payment-methods│
│ type: 'ideal'        │
└────┬─────────────────┘
     │ 2. Creates Setup Intent (seti_xxx)
     ▼
┌─────────────────┐
│ Database        │ ◄─── Saves with status=pending
│ setup_intent_id │
└────┬────────────┘
     │ 3. Returns clientSecret
     ▼
┌──────────────────────┐
│ stripe.confirmIdeal  │
│ Setup()              │
└────┬─────────────────┘
     │ 4. Redirects to bank website
     ▼
┌─────────────────┐
│   User's Bank   │ ◄─── User authorizes payment
└────┬────────────┘
     │ 5. Bank redirects back
     ▼
┌─────────────────┐
│ Your App        │ ◄─── URL: ?setup_intent=seti_xxx
└─────────────────┘
     │
     ├─────────────────────────┐
     │                         │
     ▼                         ▼
┌─────────────────┐   ┌────────────────────┐
│ Stripe Webhook  │   │ Frontend polls for │
│ (async)         │   │ status update      │
└────┬────────────┘   └────────────────────┘
     │ 6. setup_intent.succeeded
     ▼
┌─────────────────┐
│ Database        │ ◄─── Updates status=active
│                 │      Stores stripe_pm_id
└─────────────────┘
```

### Flow 3: Subscribe to Membership

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Selects "Silver Monthly"
     ▼
┌──────────────────────────────┐
│ POST /subscription-payment/  │
│      create-intent           │
└────┬─────────────────────────┘
     │ 2. Fetches pricing (€9.99)
     ▼
┌─────────────────┐
│ Stripe API      │ ◄─── Creates PaymentIntent
└────┬────────────┘
     │ 3. Returns clientSecret
     ▼
┌──────────────────────────┐
│ stripe.confirmCardPayment│
└────┬─────────────────────┘
     │ 4. User completes payment
     ▼
┌──────────────────────────────┐
│ POST /subscription-payment/  │
│      confirm                 │
└────┬─────────────────────────┘
     │ 5. Verifies payment succeeded
     ▼
┌─────────────────┐
│ Database Updates│
├─────────────────┤
│ • user_memberships.tier = silver
│ • users.subscription = silver
│ • payment_methods (if new card)
└─────────────────┘
     │ 6. Returns success
     ▼
┌─────────────────┐
│ User Dashboard  │ ◄─── Shows "Silver Member"
└─────────────────┘
```

### Flow 4: Powerbank Rental

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Scans QR code at station
     ▼
┌──────────────────────────┐
│ Check if has payment     │
│ method saved             │
└────┬─────────────────────┘
     │ YES → Continue
     │ NO  → Prompt to add card
     ▼
┌──────────────────────────────┐
│ POST /unified-payment/       │
│      create-intent           │
│ context: { type: 'rental' }  │
└────┬─────────────────────────┘
     │ 2. Creates €0.50 pre-auth
     ▼
┌─────────────────┐
│ Stripe API      │ ◄─── PaymentIntent
└────┬────────────┘
     │ 3. Charges saved payment method
     │    (off-session)
     ▼
┌─────────────────┐
│ Database        │ ◄─── Creates rental record
│ rentals table   │      Links to payment
└────┬────────────┘
     │ 4. Station unlocks slot
     ▼
┌─────────────────┐
│ User takes PB   │ ◄─── Rental starts
└────┬────────────┘
     │ ... time passes ...
     │ 5. User returns PB
     ▼
┌─────────────────┐
│ Calculate Usage │
│ • Minutes used  │
│ • Apply daily cap
│ • Check free mins
└────┬────────────┘
     │ 6. Capture final amount
     ▼
┌─────────────────┐
│ Stripe API      │ ◄─── Capture PaymentIntent
└────┬────────────┘
     │ 7. Final charge applied
     ▼
┌─────────────────┐
│ Database        │ ◄─── Updates rental.status = completed
│                 │      Updates rental.total_amount
└─────────────────┘
```

---

## Payment Method States

```
┌─────────────────────────────────────────────────────────────┐
│                    Payment Method Lifecycle                 │
└─────────────────────────────────────────────────────────────┘

CARD / SEPA:
  created → [validation charge] → active → [user deletes] → inactive

iDEAL / BANCONTACT:
  created → pending → [user redirects to bank] → [webhook] → active

Failed States:
  created → pending → [user cancels / bank declines] → failed
  created → [validation charge fails] → inactive


State Diagram:

        ┌─────────┐
        │ created │
        └────┬────┘
             │
       ┌─────┴─────┐
       │           │
    [CARD]      [iDEAL]
       │           │
       ▼           ▼
  ┌─────────┐ ┌─────────┐
  │ active  │ │ pending │
  └────┬────┘ └────┬────┘
       │           │
       │      [redirect]
       │           │
       │      [bank auth]
       │           │
       │      [webhook]
       │           │
       │           ▼
       │      ┌─────────┐
       └─────►│ active  │
              └────┬────┘
                   │
              [user deletes]
                   │
                   ▼
              ┌──────────┐
              │ inactive │
              └──────────┘
```

---

## Database Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                  Database Entity Relationships               │
└─────────────────────────────────────────────────────────────┘

users
  │
  ├──► payment_methods (1:many)
  │     • user_id → users.user_id
  │     • stripe_payment_method_id → Stripe PM
  │     • stripe_customer_id → Stripe Customer
  │
  ├──► user_memberships (1:1)
  │     • user_id → users.user_id
  │     • stripe_subscription_id → Stripe Subscription
  │
  ├──► rentals (1:many)
  │     • user_id → users.user_id
  │     • stripe_payment_intent_id → Stripe Payment
  │
  └──► billing_transactions (1:many)
        • user_id → users.user_id
        • stripe_payment_intent_id → Stripe Payment


payment_methods Table Structure:

┌──────────────────────────────────────────────┐
│ id          | INTEGER PRIMARY KEY            │
│ user_id     | TEXT NOT NULL → users.user_id  │
│ type        | TEXT (card, ideal, sepa, etc.) │
│ stripe_pm_id| TEXT → Stripe PaymentMethod    │
│ is_primary  | BOOLEAN                        │
│ status      | pending | active | failed      │
│ supports_*  | BOOLEAN flags                  │
└──────────────────────────────────────────────┘
        │
        │ RLS Policies:
        ├─► SELECT: auth.uid() = user_id
        ├─► INSERT: auth.uid() = user_id
        ├─► UPDATE: auth.uid() = user_id
        └─► DELETE: auth.uid() = user_id
```

---

## Security Model

```
┌─────────────────────────────────────────────────────────────┐
│                      Security Layers                         │
└─────────────────────────────────────────────────────────────┘

FRONTEND
  │
  ├─► PCI Compliance
  │   • No raw card data stored
  │   • Stripe Elements handles sensitive data
  │   • Data sent directly to Stripe
  │
  ├─► HTTPS Only
  │   • All connections encrypted
  │   • Stripe requires TLS 1.2+
  │
  └─► JWT Authentication
      • All API calls require valid token
      • Token validates with Supabase Auth


EDGE FUNCTIONS
  │
  ├─► Authentication
  │   • Validates JWT token
  │   • Extracts user ID from token
  │   • Rejects unauthenticated requests
  │
  ├─► Authorization
  │   • Checks user owns payment method
  │   • Validates user_id matches auth.uid()
  │   • Rejects unauthorized access
  │
  ├─► Webhook Verification
  │   • Validates Stripe signature
  │   • Checks timestamp (prevents replay)
  │   • Uses HMAC SHA-256
  │
  └─► Stripe API Validation
      • Validates Payment Method IDs
      • Verifies payment method belongs to customer
      • Checks payment status before confirming


DATABASE
  │
  ├─► Row Level Security (RLS)
  │   • SELECT: user_id = auth.uid()
  │   • INSERT: user_id = auth.uid()
  │   • UPDATE: user_id = auth.uid()
  │   • DELETE: user_id = auth.uid()
  │
  ├─► Data Encryption
  │   • Encrypted at rest
  │   • Encrypted in transit
  │   • No sensitive card data stored
  │
  └─► Service Role Key
      • Edge functions use service role
      • Bypasses RLS for webhook updates
      • Validates user ownership first
```

---

## Payment Method Icons

```
┌─────────────────────────────────────────────────┐
│           Payment Method Card Designs           │
└─────────────────────────────────────────────────┘

VISA
┌─────────────────────────────────────────────┐
│ 🔵 Blue Gradient                            │
│                                             │
│ VISA                           💳           │
│ **** **** **** 4242                        │
│ John Doe                                    │
└─────────────────────────────────────────────┘

MASTERCARD
┌─────────────────────────────────────────────┐
│ 🔴 Red/Orange Gradient                      │
│                                             │
│                               🔴🟡          │
│ **** **** **** 5555                        │
│ Jane Smith                                  │
└─────────────────────────────────────────────┘

iDEAL
┌─────────────────────────────────────────────┐
│ 🟦 Blue Gradient                            │
│                                             │
│ iDEAL                          [iDEAL]      │
│ **** **** **** iDEAL                       │
│ Dutch User                                  │
└─────────────────────────────────────────────┘

SEPA
┌─────────────────────────────────────────────┐
│ 🟦 Dark Blue Gradient                       │
│                                             │
│ SEPA Direct Debit              [SEPA]      │
│ **** **** **** 3184                        │
│ German User                                 │
└─────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   Error Handling Strategy                    │
└─────────────────────────────────────────────────────────────┘

User Action
    │
    ▼
Try Payment
    │
    ├──► Success → Continue
    │
    └──► Error
         │
         ├─► Network Error
         │   └─► "Unable to connect. Check your internet."
         │
         ├─► Authentication Error (401)
         │   └─► "Please log in to continue"
         │
         ├─► Stripe Error
         │   ├─► card_declined
         │   │   └─► "Your card was declined. Try another card."
         │   ├─► insufficient_funds
         │   │   └─► "Insufficient funds. Try another card."
         │   ├─► expired_card
         │   │   └─► "Your card has expired."
         │   └─► Other
         │       └─► Show Stripe error message
         │
         ├─► Validation Error (400)
         │   └─► Display specific field errors
         │
         └─► Server Error (500)
             └─► "Something went wrong. Please try again."
                 (Error logged for debugging)


Logging Strategy:

Frontend:
  console.error('Payment error:', error);
  // Shows in browser console

Backend:
  console.error('Edge function error:', error);
  // Visible in Supabase Edge Function logs

Stripe:
  // All events logged in Stripe Dashboard
  // Webhooks show delivery status
  // Failed payments show decline codes
```

---

**Created:** December 9, 2025
**Last Updated:** December 9, 2025
